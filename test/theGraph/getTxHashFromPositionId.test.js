const chains = require("../../src/config/chains");
const queryTheGraph = require("../../src/utils/queryTheGraph");

const validPosition = {
  id: 482139,
  chain: 1,
};
describe("TheGraph - get Tx Hash From Position Id", () => {
  test("should retreive valid tx hash for position id", async () => {
    expect(1).toEqual(1);
  });
});

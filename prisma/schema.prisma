// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Position {
  id                    Int  @id
  createdAt             DateTime?
  initValueToken0       Float?
  initValueToken1       Float?
  initToken0USDRate     Float?
  initToken1USDRate     Float?
  initPriceT1T2         Float?
  dataPoints            PositionInfo[]
}

model PositionInfo {
  id                    Int  @id @default(autoincrement())
  positionId            Position @relation(fields: [inter_pos_id], references: [id])
  inter_pos_id          Int 
  createdAt             DateTime @default(now())
  pair                  String
  liquidityToken0       Float
  liquidityToken1       Float     
  feesToken0            Float
  feesToken1            Float 
  priceToken0           Float
  etherUsdExchangeRate  Float
  ArbitUsdExchangeRate  Float
  blockNumber           Int
}
